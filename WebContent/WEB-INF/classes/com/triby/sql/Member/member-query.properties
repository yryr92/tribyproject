signupMember = INSERT INTO MEMBER VALUES(SEQ_UNO.NEXTVAL,?,?,?,?,0,?,'user.png',?,0,SYSDATE,SYSDATE,'N','N')
loginMember = SELECT * FROM MEMBER WHERE USER_EMAIL=? AND USER_PW = ? AND USER_STATUS ='N'
emailCheck = SELECT COUNT(*) FROM MEMBER WHERE USER_EMAIL=?
findID = SELECT USER_EMAIL FROM MEMBER WHERE USER_NAME=? AND USER_PHONE=?
findPwd = SELECT COUNT(*)FROM MEMBER WHERE USER_EMAIL=? AND USER_NAME=? AND USER_PHONE=?
#########################from min 10/05
#member-query.properties
adminGetListCount = SELECT COUNT(*) FROM MEMBER
adminSelectMember = SELECT * FROM (SELECT ROWNUM AS RNUM, M.* FROM (SELECT USER_NO,USER_EMAIL,USER_PW,USER_NAME,USER_GENDER,USER_BIRTH,USER_PHONE,USER_PROFILE,USER_CATEGORY,USER_REPORTED_COUNT,USER_REGISTER_DATE,MODIFY_DATE,USER_STATUS FROM MEMBER WHERE USER_NO NOT LIKE '2%' ORDER BY USER_NO DESC)M) WHERE RNUM BETWEEN ? AND ?
adminSelectMemberStatus = SELECT * FROM (SELECT ROWNUM AS RNUM, M.* FROM (SELECT USER_NO,USER_EMAIL,USER_PW,USER_NAME,USER_GENDER,USER_BIRTH,USER_PHONE,USER_PROFILE,USER_CATEGORY,USER_REPORTED_COUNT,USER_REGISTER_DATE,MODIFY_DATE,USER_STATUS FROM MEMBER WHERE USER_STATUS='Y' AND USER_NO NOT LIKE '2%' ORDER BY USER_NO DESC)M) WHERE RNUM BETWEEN ? AND ?
adminSearchUserNoList = SELECT * FROM MEMBER WHERE USER_NO LIKE ? AND USER_STATUS='N' AND USER_NO NOT LIKE '2%'
adminSearchUserNameList = SELECT * FROM MEMBER WHERE USER_NAME LIKE ? AND USER_STATUS='N' AND USER_NO NOT LIKE '2%'
##############################from pak 10/05
updatePwd= UPDATE MEMBER SET USER_PW=? WHERE USER_EMAIL=? 

######################insub
userProfile=SELECT * FROM MEMBER WHERE USER_NO=?
couponCount=SELECT COUNT(*) FROM COUPON WHERE USER_NO=? AND COUPON_STATUS='N' AND (COUPON_END - SYSDATE)>-1
selectLikeTribyList=SELECT * FROM(SELECT ROWNUM AS RNUM,L.* FROM (SELECT USER_NO, TRIBY_NO, TRIBY_TITLE, TRIBY_PRICE,HOST_NO, IMG_NAME FROM LIKE_TRIBY JOIN TRIBY USING(TRIBY_NO) JOIN T_IMAGE USING(TRIBY_NO) WHERE USER_NO=? ORDER BY 2 DESC) L) WHERE RNUM BETWEEN ? AND ?
myLikeTribyCount=SELECT COUNT(*) FROM(SELECT USER_NO, TRIBY_NO, TRIBY_TITLE, TRIBY_PRICE,HOST_NO, IMG_NAME FROM LIKE_TRIBY JOIN TRIBY USING(TRIBY_NO) JOIN T_IMAGE USING(TRIBY_NO) WHERE USER_NO=?)
selectLikeHostList=SELECT * FROM(SELECT ROWNUM AS RNUM, H.* FROM(SELECT USER_NO, HOST_NO, HOST_SNAME, HOST_IMAGE FROM LIKE_HOST JOIN HOST USING(HOST_NO) WHERE USER_NO=? AND STATUS ='N') H ORDER BY RNUM DESC) WHERE RNUM BETWEEN ? AND ?
hostTribyCount=SELECT COUNT(*) FROM TRIBY WHERE HOST_NO=? AND APPROVAL_STATUS='Y'
hostReviewCount=SELECT COUNT(*) FROM REVIEW WHERE HOST_NO=? AND REVIEW_STATUS='Y'
hostLikeCount=SELECT COUNT(*) FROM LIKE_HOST WHERE HOST_NO=?
myLikeHostCount=SELECT COUNT(*) FROM LIKE_HOST WHERE USER_NO=?
selectMyCouponList=SELECT * FROM COUPON WHERE USER_NO=? AND COUPON_STATUS='N'AND (COUPON_END - SYSDATE)>-1
selectCategoryList=SELECT CATEGORY_NAME FROM CATEGORY
updateMyCategory=UPDATE MEMBER SET USER_CATEGORY=? WHERE USER_NO=?
selectTribyPreList=SELECT * FROM(SELECT ROWNUM AS RNUM,A.* FROM (SELECT TRIBY_ID, TRIBY_TITLE, ADDRESS, IMG_NAME,TTIME, TRIBY_PRICE,(PAYMENT_FINAL+PAYMENT_DISCO)/TRIBY_PRICE AS AMOUNT,TO_CHAR(PAYMENT_DATE,'YYYY/MM/DD'), TOP.STATUS,PAYMENT_NO,HOST_NO,HOST_SNAME FROM PAYMENT P JOIN T_OPTION TOP USING(TRIBY_OP_NO) JOIN TRIBY T ON(TRIBY_ID=TRIBY_NO) JOIN T_IMAGE TI USING(TRIBY_NO) JOIN HOST USING(HOST_NO) WHERE USER_NO = ? AND TOP.STATUS='N' AND IMG_LEV=1 AND P.STATUS='Y' ORDER BY PAYMENT_NO DESC) A ) WHERE RNUM BETWEEN ? AND ?
selectTribyPreListCount=SELECT COUNT(*) FROM (SELECT TRIBY_ID, TRIBY_TITLE, ADDRESS, IMG_NAME,TTIME, TRIBY_PRICE,(PAYMENT_FINAL+PAYMENT_DISCO)/TRIBY_PRICE AS AMOUNT,PAYMENT_DATE, TOP.STATUS FROM PAYMENT P JOIN T_OPTION TOP USING(TRIBY_OP_NO) JOIN TRIBY T ON(TRIBY_ID=TRIBY_NO) JOIN T_IMAGE TI USING(TRIBY_NO) WHERE USER_NO = ? AND TOP.STATUS='N' AND IMG_LEV=1 ORDER BY PAYMENT_DATE DESC)
selectTribyHistoryList=SELECT * FROM(SELECT ROWNUM AS RNUM,A.* FROM (SELECT TRIBY_ID, TRIBY_TITLE, ADDRESS, IMG_NAME,TTIME, TRIBY_PRICE,(PAYMENT_FINAL+PAYMENT_DISCO)/TRIBY_PRICE AS AMOUNT,TO_CHAR(PAYMENT_DATE,'YYYY/MM/DD'), TOP.STATUS,PAYMENT_NO,HOST_NO,HOST_SNAME FROM PAYMENT P JOIN T_OPTION TOP USING(TRIBY_OP_NO) JOIN TRIBY T ON(TRIBY_ID=TRIBY_NO) JOIN T_IMAGE TI USING(TRIBY_NO) JOIN HOST USING(HOST_NO)WHERE USER_NO = ? AND IMG_LEV=1 ORDER BY PAYMENT_NO DESC) A ) WHERE RNUM BETWEEN ? AND ?
selectTribyHistoryListCount=SELECT COUNT(*) FROM (SELECT TRIBY_ID, TRIBY_TITLE, ADDRESS, IMG_NAME,TTIME, TRIBY_PRICE,(PAYMENT_FINAL+PAYMENT_DISCO)/TRIBY_PRICE AS AMOUNT,PAYMENT_DATE, TOP.STATUS FROM PAYMENT P JOIN T_OPTION TOP USING(TRIBY_OP_NO) JOIN TRIBY T ON(TRIBY_ID=TRIBY_NO) JOIN T_IMAGE TI USING(TRIBY_NO) WHERE USER_NO = ? AND IMG_LEV=1 ORDER BY PAYMENT_DATE DESC)
selectReviewTarget=SELECT TRIBY_ID, TRIBY_TITLE, ADDRESS, IMG_NAME,TTIME, TRIBY_PRICE,(PAYMENT_FINAL+PAYMENT_DISCO)/TRIBY_PRICE AS AMOUNT,TO_CHAR(PAYMENT_DATE,'YYYY/MM/DD'), TOP.STATUS,PAYMENT_NO,HOST_NO,HOST_SNAME FROM PAYMENT P JOIN T_OPTION TOP USING(TRIBY_OP_NO) JOIN TRIBY T ON(TRIBY_ID=TRIBY_NO) JOIN T_IMAGE TI USING(TRIBY_NO) JOIN HOST USING(HOST_NO) WHERE USER_NO = ? AND TOP.STATUS='E' AND IMG_LEV=1 AND PAYMENT_NO=?
selectMyReviewList=SELECT * FROM(SELECT ROWNUM AS RNUM, RV.* FROM(SELECT R.REVIEW_NO,PAYMENT_NO,R.HOST_NO,TRIBY_NO,(PAYMENT_FINAL+PAYMENT_DISCO)/TRIBY_PRICE AS AMOUNT,TRIBY_TITLE,ADDRESS,IMG_NAME,HOST_SNAME,REVIEW_CONTENT,REVIEW_POINT,TDAY,REVIEW_DATE,REVIEW_REPLY FROM REVIEW R JOIN PAYMENT USING(PAYMENT_NO)JOIN T_OPTION USING(TRIBY_OP_NO)JOIN TRIBY ON(TRIBY_ID=TRIBY_NO)JOIN HOST H ON(R.HOST_NO=H.HOST_NO)JOIN T_IMAGE USING(TRIBY_NO)WHERE REVIEW_STATUS='Y' AND R.USER_NO=? AND IMG_LEV=1) RV)WHERE RNUM BETWEEN ? AND ?
selectMyReviewListCount=SELECT COUNT(*) FROM REVIEW WHERE REVIEW_STATUS='Y' AND USER_NO=?
canceledTribySearch=SELECT STATUS FROM PAYMENT WHERE PAYMENT_NO=? AND STATUS='N'
tribyCancel=UPDATE PAYMENT SET STATUS='N' WHERE PAYMENT_NO=?
removeLikeTriby=DELETE FROM LIKE_TRIBY WHERE USER_NO=? AND TRIBY_NO=?
addLikeTriby=INSERT INTO LIKE_TRIBY VALUES(?,?)
removeLikeHost=DELETE FROM LIKE_HOST WHERE USER_NO=? AND HOST_NO=?
addLikeHost=INSERT INTO LIKE_HOST VALUES(?,?)
updateProfileImage=UPDATE MEMBER SET USER_PROFILE=? WHERE USER_NO=?
updateMyPw=UPDATE MEMBER SET USER_PW=? WHERE USER_NO=?
selectPoint=SELECT AVG(REVIEW_POINT) FROM REVIEW WHERE HOST_NO=?
amIlikeHost=SELECT COUNT(*) FROM LIKE_HOST WHERE USER_NO=? AND HOST_NO=?
amIlikeTriby=SELECT COUNT(*) FROM LIKE_TIRBY WHERE USER_NO? AND TRIBY_NO=?
decreseTOcount=UPDATE T_OPTION SET TRIBY_ATTEND=TRIBY_ATTEND-(SELECT (PAYMENT_FINAL+PAYMENT_DISCO)/TRIBY_PRICE FROM PAYMENT JOIN T_OPTION USING(TRIBY_OP_NO) JOIN TRIBY ON(TRIBY_ID=TRIBY_NO)WHERE PAYMENT_NO=?) WHERE TRIBY_OP_NO=(SELECT TRIBY_OP_NO FROM T_OPTION JOIN PAYMENT USING(TRIBY_OP_NO) WHERE PAYMENT_NO=?)
increseHostLikeCount=UPDATE HOST SET LIKE_COUNT=LIKE_COUNT+1 WHERE HOST_NO=?
decreseHostLikeCount=UPDATE HOST SET LIKE_COUNT=LIKE_COUNT-1 WHERE HOST_NO=?
amIlikeTriby=SELECT COUNT(*) FROM LIKE_TRIBY WHERE USER_NO=? AND TRIBY_NO=?
selectMyQnaList=SELECT * FROM(SELECT ROWNUM AS RNUM,Q.* FROM(SELECT QNA_H_NO,HOST_NO,HOST_SNAME,QNA_H_TITLE,TO_CHAR(QNA_H_DATE,'YYYY/MM/DD'),QNA_H_STATUS,HOST_IMAGE FROM QNA JOIN HOST USING(HOST_NO) WHERE USER_NO=? ORDER BY QNA_H_NO DESC) Q) WHERE RNUM BETWEEN ? AND ?
myQnaListCount=SELECT COUNT(*) FROM QNA WHERE USER_NO=?
loadMyQcontent=SELECT QNA_H_TITLE,QNA_H_Q,HOST_SNAME,QNA_H_A,TO_CHAR(QNA_H_ADATE,'YYYY/MM/DD'),QNA_H_STATUS FROM QNA JOIN HOST USING(HOST_NO) WHERE QNA_H_NO=?
qnaHostName=SELECT HOST_SNAME FROM HOST WHERE HOST_NO=?
